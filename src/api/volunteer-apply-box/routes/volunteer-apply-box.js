'use strict';

/**
 * volunteer-apply-box router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::volunteer-apply-box.volunteer-apply-box');
